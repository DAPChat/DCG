using packets;
using game;

namespace card
{
    public class Zeno : BaseCard
    {
        public Zeno(CAP _action, Game _game)
        {
            action = _action;
            game = _game;
        }
    }
}