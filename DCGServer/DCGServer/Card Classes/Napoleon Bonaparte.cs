using packets;
using game;

namespace card
{
    public class Napoleon_Bonaparte : BaseCard
    {
        public Napoleon_Bonaparte(CAP _action, Game _game)
        {
            action = _action;
            game = _game;
        }
    }
}